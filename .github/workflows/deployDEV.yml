name: Laravel

on:
  push:
    branches: [ "dev.unreal-go.top" ]
  pull_request:
    branches: [ "dev.unreal-go.top" ]

jobs:
 prepare:
    runs-on: ubuntu-latest
    steps:
    - uses: shivammathur/setup-php@15c43e89cdef867065b0213be354c2841860869e
      with:
        php-version: '8.0'
        fetch-depth: 2
    - uses: actions/checkout@v3

    - name: Configure Public folder
      run: ls -l ~



